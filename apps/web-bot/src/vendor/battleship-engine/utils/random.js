// packages/battleship-engine/src/utils/random.js

// TODO: random helpers for ship placement and move selection.
export function randomInt(maxExclusive) {
  return Math.floor(Math.random() * maxExclusive);
}
