// packages/battleship-engine/src/rules/placement.js

// TODO: implement ship placement helpers (random placement, manual placement validation, etc.).
export function placeShips(board, ships) {
  // Placeholder implementation.
  return { board, ships };
}
